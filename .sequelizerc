const path = require('path');

console.log('Loading Sequelize configuration for environment: ', process.env.NODE_ENV);
module.exports = {
  'config': path.resolve('database/config/config.js'), // Ruta a tu archivo de configuración de Sequelize
  'models-path': path.resolve('database/models'),         // Ruta a la carpeta de modelos
  'migrations-path': path.resolve('database/migrations'), // Ruta a la carpeta de migraciones
  'seeders-path': path.resolve('database/seeders'),       // Ruta a la carpeta de seeders (si la usas)
};